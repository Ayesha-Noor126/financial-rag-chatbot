# Test-only dependencies — NOT installed in the production image.
# Install with: pip install -r requirements-dev.txt
# (after requirements.txt is already installed)

pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==6.0.0
# httpx is already in requirements.txt (runtime dep); listed here for clarity
httpx==0.27.2
